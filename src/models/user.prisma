model User {
  id           Int    @id @default(autoincrement())
  username     String @unique
  password     String
  refreshToken String
  isRevoked    Boolean @default(false)
  todos        Todo[]

  @@map("users")
}
