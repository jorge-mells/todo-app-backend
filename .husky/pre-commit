#!/usr/bin/env sh

echo "🛡️  Starting Pre-Commit Quality Gate..."

# 1. Linting
echo "🔍 Running Lint..."
npm run lint || { echo "❌ Lint failed. Fix errors before committing."; exit 1; }

# # 2. Build(too slow, don't do this yet)
# echo "🏗️  Verifying Build..."
# docker compose build || { echo "❌ Build failed. Check your Dockerfile."; exit 1; }

# 3. Tests
echo "🧪 Running Tests..."
npm run test || { echo "❌ Tests failed. Committing blocked."; exit 1; }

echo "✅ All checks passed. Proceeding with commit."
